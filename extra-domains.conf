# Extra domains for devbox firewall
# One domain per line, # for comments
#
# Docker Hub and npm are already in base domains (init-firewall.sh).
# Add project-specific domains here, or pass via DEVBOX_EXTRA_DOMAINS env var.

# Python (pip/uv)
pypi.org
files.pythonhosted.org

# Rust (cargo)
crates.io
static.crates.io

# pnpm mirror (if needed)
# registry.npmmirror.com
